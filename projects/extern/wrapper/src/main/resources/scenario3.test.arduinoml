application redButton

# Declaring bricks
sensor button: 9
actuator ll: 12
# Declaring states
state {
    ll <= HIGH
    button is HIGH => start
}

-> start {
    ll <= LOW
    button is HIGH => state
}

